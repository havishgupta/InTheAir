<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Plan Route &amp; Download Offline Maps"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <AutoCompleteTextView
        android:id="@+id/etFrom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Departure (City or Airport)"
        android:inputType="textCapWords"
        android:completionThreshold="2"
        android:layout_marginBottom="8dp"/>

    <AutoCompleteTextView
        android:id="@+id/etTo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Destination (City or Airport)"
        android:inputType="textCapWords"
        android:completionThreshold="2"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/btnDownload"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Download Route Maps"
        android:backgroundTint="#007AFF"
        android:textColor="#FFFFFF"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Or download current view:"
        android:textSize="14sp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"/>

    <Button
        android:id="@+id/btnDownloadManual"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Manual Download (Select Zoom Boxes)"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"/>

</LinearLayout>